name: snapshot

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  
  issue_comment:
    types: [created]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - run: |
          echo ${{ github.event.issue_comment.body }}